# Example 5 Validation Policy

This file is retained as historical context only.

The canonical Example 5 validation policy now lives in:

- `/home/dgketchum/code/swim-rs/examples/VALIDATION_POLICY.md`

That consolidated document is the maintained source of truth for:

- Example 5 `no_mask` validation policy
- paired-comparison rules
- publication-period rules
- canonical reporting requirements

Do not update this file with new policy logic. Update the consolidated
policy instead.
